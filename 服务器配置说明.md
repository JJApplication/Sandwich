# Sandwich 服务器配置说明

## 最大请求体大小配置

### 概述

Sandwich 代理服务器现在支持配置最大请求体大小，这是一个重要的安全和性能特性，可以防止恶意的大文件上传攻击，并控制内存使用。

### 配置项说明

在服务器配置中新增了 `max_request_body` 字段：

```json
{
  "servers": [
    {
      "name": "http-server",
      "host": "0.0.0.0",
      "port": 80,
      "protocol": "http",
      "enabled": true,
      "max_request_body": 33554432,  // 32MB，单位是字节
      "use_http2": false
    }
  ]
}
```

### 配置说明

- **字段名称**: `max_request_body`
- **数据类型**: `int64` 
- **单位**: 字节（bytes）
- **默认值**: 33554432（32MB）
- **最小值**: 0（表示使用默认值）
- **作用**: 限制单个HTTP请求体的最大大小

### 常用配置值

| 大小描述 | 字节值 | 适用场景 |
|---------|--------|----------|
| 1MB     | 1048576 | 轻量级API服务 |
| 10MB    | 10485760 | 一般Web应用 |
| 32MB    | 33554432 | 默认值，适合大多数场景 |
| 64MB    | 67108864 | 需要上传大文件的服务 |
| 100MB   | 104857600 | 文件上传服务 |
| 512MB   | 536870912 | 大文件处理服务 |

### 配置示例

#### 1. 不同服务不同限制

```json
{
  "servers": [
    {
      "name": "api-server",
      "port": 8080,
      "max_request_body": 10485760,  // 10MB，API服务限制较小
      "enabled": true
    },
    {
      "name": "upload-server", 
      "port": 8081,
      "max_request_body": 104857600, // 100MB，文件上传服务
      "enabled": true
    },
    {
      "name": "admin-server",
      "port": 8082,
      "max_request_body": 67108864,  // 64MB，管理后台
      "enabled": true
    }
  ]
}
```

#### 2. HTTPS 和 HTTP 不同配置

```json
{
  "servers": [
    {
      "name": "http-server",
      "port": 80,
      "protocol": "http",
      "max_request_body": 33554432,  // 32MB
      "enabled": true
    },
    {
      "name": "https-server",
      "port": 443, 
      "protocol": "https",
      "max_request_body": 67108864,  // 64MB，HTTPS更安全，可以允许更大的请求
      "enabled": true,
      "tls": {
        "cert_file": "/path/to/cert.pem",
        "key_file": "/path/to/key.pem"
      }
    }
  ]
}
```

### 实现原理

1. **Content-Length 检查**: 优先检查HTTP请求的Content-Length头
   - 如果Content-Length超出限制，立即返回HTTP 413状态码
   - 避免接收任何请求体数据，节省带宽和处理时间

2. **动态读取限制**: 对于没有Content-Length头的请求
   - 使用自定义的`limitedReader`在读取过程中监控
   - 一旦读取的数据超出限制，立即停止并返回错误
   - 防止恶意的分块传输攻击

3. **优雅处理**: 超过限制时的处理方式
   - HTTP状态码：413 Payload Too Large
   - 响应头：`Connection: close`
   - 服务器日志：记录超限请求的详细信息
   - 连接：自动关闭以节省资源

4. **内存保护**: 请求体大小控制的安全机制
   - 防止大请求消耗过多服务器内存
   - 避免请求体被截断导致的数据不完整问题
   - 确保服务器稳定性和响应能力

### 错误处理

当请求体超过配置的最大大小时：

**情况一：Content-Length检查**
- 触发条件：请求包含Content-Length头且值超出限制
- HTTP状态码：413 Payload Too Large
- 响应头：`Connection: close`
- 响应体："Request entity too large"
- 处理方式：立即拒绝，不接收任何请求体数据

**情况二：动态读取检查**
- 触发条件：没有Content-Length头或为-1，在读取过程中超限
- HTTP状态码：由上层处理器决定（通常为400或500）
- 错误信息："request entity too large"
- 处理方式：在读取过程中检测并立即停止

**日志记录**
- 记录超限请求的Host、大小和限制值
- 便于监控和故障排查
- 帮助识别恶意攻击模式

### 性能影响

- **内存使用**: 有效控制内存消耗，防止OOM
- **响应时间**: 大请求会被快速拒绝，不影响其他请求
- **网络带宽**: 减少无效的大文件传输
- **服务稳定性**: 提高服务器的整体稳定性

### 监控建议

1. **日志监控**: 监控413错误的频率
2. **性能监控**: 观察内存使用情况
3. **业务监控**: 确保合法的大文件上传不受影响

### 注意事项

1. **配置合理**: 根据业务需求设置合适的大小
2. **客户端处理**: 确保客户端能正确处理413错误
3. **负载均衡**: 如果使用负载均衡，也需要配置对应的限制
4. **分片上传**: 对于大文件，建议使用分片上传

### 故障排除

#### 问题1：合法请求被拒绝
- **症状**: 正常的文件上传失败
- **解决**: 增加 `max_request_body` 的值

#### 问题2：服务器内存占用过高
- **症状**: 内存使用持续增长
- **解决**: 减少 `max_request_body` 的值

#### 问题3：配置不生效
- **症状**: 大请求仍然被接受
- **解决**: 检查配置文件格式，重启服务

### 更新步骤

1. 修改配置文件中的 `max_request_body` 值
2. 验证配置文件格式正确
3. 重启 Sandwich 服务
4. 测试验证配置是否生效

### 相关文件

- `config/config.go`: 配置结构定义
- `server/manager.go`: 服务器管理和配置应用
- `config.server.example.json`: 配置示例文件