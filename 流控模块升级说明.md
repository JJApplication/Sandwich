# Sandwich 流控模块使用说明

## 概述

Sandwich 流控模块已升级，支持基于host、请求头的精准匹配和多时间范围的流控策略。新版本完全向后兼容，同时提供了强大的新功能。

## 主要特性

### 1. 精准匹配规则
- **Host匹配**: 根据请求的Host头进行限流
- **请求头匹配**: 根据任意HTTP请求头进行限流（如User-Agent、Referer等）
- **IP匹配**: 根据客户端IP进行限流

### 2. 多时间窗口限流
- 支持同时配置多个时间窗口
- 支持秒(s)和分钟(min)时间单位
- 示例：`10/100s` 表示100秒内允许10个请求，`100/10min` 表示10分钟内允许100个请求

### 3. 流控记录
- 自动记录被限流的请求信息
- 支持多种存储方式：InfluxDB、MongoDB、文件
- 记录详细信息：Host、IP、User-Agent、URL、时间等

## 配置说明

### 基本配置结构

```json
{
  "flow_control": {
    "enabled": true,
    "global_limit": {
      "requests": 1000,
      "window": "60s", 
      "unit": "s"
    },
    "rules": [
      // 规则列表
    ],
    "recording": {
      "enabled": true,
      "record_blocked": true,
      "record_allowed": false,
      "storage_type": "influx",
      "retention_period": "30d"
    }
  }
}
```

### 规则配置详解

```json
{
  "name": "api-limit",
  "enabled": true,
  "priority": 1,              // 优先级，数字越小优先级越高
  "match_type": "host",       // 匹配类型: host, header, ip
  "match_value": "api.example.com",  // 匹配值
  "header_key": "",           // 当match_type为header时的header键名
  "limits": [
    {
      "requests": 10,         // 允许的请求数
      "window": "100s",       // 时间窗口
      "unit": "s"            // 时间单位: s, min
    },
    {
      "requests": 100,
      "window": "10min",
      "unit": "min"
    }
  ],
  "action": "block",          // 动作: block, delay
  "description": "API服务限流"
}
```

## 配置示例

### 1. 基于Host的限流

```json
{
  "name": "api-strict-limit",
  "enabled": true,
  "priority": 1,
  "match_type": "host",
  "match_value": "api.example.com",
  "limits": [
    {"requests": 10, "window": "100s", "unit": "s"},
    {"requests": 100, "window": "10min", "unit": "min"}
  ],
  "action": "block",
  "description": "API服务严格限流"
}
```

### 2. 基于User-Agent的限流

```json
{
  "name": "bad-bot-block",
  "enabled": true,
  "priority": 2,
  "match_type": "header",
  "header_key": "User-Agent",
  "match_value": "BadBot",
  "limits": [
    {"requests": 1, "window": "60s", "unit": "s"}
  ],
  "action": "block",
  "description": "封禁恶意爬虫"
}
```

### 3. 基于IP的限流

```json
{
  "name": "suspicious-ip-block",
  "enabled": true,
  "priority": 5,
  "match_type": "ip",
  "match_value": "192.168.1.100",
  "limits": [
    {"requests": 2, "window": "120s", "unit": "s"}
  ],
  "action": "block",
  "description": "可疑IP限流"
}
```

## 使用步骤

### 1. 更新配置文件

在你的 `config.json` 中添加 `flow_control` 配置项，参考 `config.flow.example.json` 文件。

### 2. 重启服务

```bash
go build -o sandwich sandwich.go
./sandwich
```

### 3. 监控日志

流控事件会记录在日志中：
```
Flow blocked: Host=api.example.com, IP=192.168.1.1, Rule=api-limit, Reason=Rule 'api-limit' rate limit exceeded
```

### 4. 查看限流记录

根据配置的存储类型查看记录：
- **文件存储**: 查看 `flow_records.json` 文件
- **InfluxDB**: 查询 flow_blocked 指标
- **MongoDB**: 查询对应的集合

## 高级功能

### 1. 优先级控制

规则按priority字段排序执行，数字越小优先级越高。高优先级规则会先执行，一旦匹配就不会继续检查后续规则。

### 2. 动态配置更新

目前需要重启服务来更新配置，后续版本会支持热更新。

### 3. 性能优化

- 使用内存缓存减少配置解析开销
- 定期清理过期记录避免内存泄漏
- 支持并发请求处理

## 故障排除

### 1. 配置错误

检查 JSON 格式是否正确，特别注意：
- 时间单位只支持 "s" 和 "min"
- match_type 只支持 "host", "header", "ip"
- 当 match_type 为 "header" 时必须设置 header_key

### 2. 性能问题

如果请求量很大，建议：
- 减少规则数量
- 增加时间窗口大小
- 关闭不必要的记录功能

### 3. 存储问题

- **InfluxDB**: 确保InfluxDB服务正常运行且配置正确
- **文件存储**: 确保有写入权限
- **MongoDB**: 确保MongoDB连接正常

## 迁移指南

从旧版本迁移到新版本：

1. **配置迁移**: 旧的 middleware.limiter 配置仍然有效，但建议迁移到新的 flow_control 配置
2. **API兼容**: 旧的 `flow.GetLimiter().GetConn()` 调用仍然有效，但已标记为废弃
3. **渐进式升级**: 可以先启用新配置，然后逐步移除旧配置

## 示例文件

参考项目中的以下文件：
- `config.flow.example.json`: 完整的配置示例
- `test/flow_test.go`: 测试程序示例
- `流控模块升级说明.md`: 本文档