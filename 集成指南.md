# Sandwich v2 集成指南

## 概述

Sandwich v2 是对原有代理服务的重大升级，新增了多端口监听、HTTP/3 和 WebSocket 协议支持，同时保持了与 v1 版本的兼容性。

## 新功能特性

### 1. 多端口监听
- 支持同时监听多个端口（如 80 和 443）
- 每个端口可以配置不同的协议和域名绑定
- 支持 TLS 证书配置

### 2. HTTP/3 协议支持
- 基于 QUIC 协议的 HTTP/3 服务器
- 自动协议协商和降级
- 可选特性，通过配置启用

### 3. WebSocket 协议支持
- WebSocket 连接的代理转发
- 双向数据流处理
- 连接保持和心跳机制
- 可选特性，通过配置启用

### 4. 配置管理系统
- 统一的 YAML 配置文件
- 环境变量覆盖支持
- 配置热重载
- 配置验证和一致性检查

## 文件结构

```
Sandwich/
├── sandwich.go              # 原版本主文件
├── sandwich_v2.go           # 新版本主文件
├── proxy.go                 # 原有代理逻辑
├── config/                  # 配置管理包
│   ├── config.go           # 配置结构定义
│   ├── loader.go           # 配置加载器
│   ├── validator.go        # 配置验证器
│   ├── env_mapper.go       # 环境变量映射
│   ├── utils.go            # 配置工具函数
│   └── example.yaml        # 配置示例文件
├── server/                  # 服务器管理包
│   ├── manager.go          # 服务器管理器
│   └── router.go           # 请求路由器
├── protocols/               # 协议支持包
│   ├── http3/
│   │   └── server.go       # HTTP/3 服务器
│   └── websocket/
│       └── server.go       # WebSocket 服务器
├── 需求设计文档.md          # 需求设计文档
├── 开发任务列表.md          # 开发任务列表
└── 集成指南.md              # 本文档
```

## 使用方法

### 1. 使用默认配置启动

```bash
# 使用默认配置启动 v2 版本
go run sandwich_v2.go

# 或者编译后运行
go build -o sandwich_v2 sandwich_v2.go
./sandwich_v2
```

### 2. 使用配置文件启动

```bash
# 使用指定配置文件启动
go run sandwich_v2.go --config=config/example.yaml

# 或者
./sandwich_v2 --config=config/example.yaml
```

### 3. 查看版本信息

```bash
./sandwich_v2 --version
```

### 4. 查看帮助信息

```bash
./sandwich_v2 --help
```

## 配置文件示例

### 基本配置

```yaml
# 服务器配置
servers:
  - name: "http-server"
    host: "0.0.0.0"
    port: 80
    protocol: "http"
    enabled: true
  
  - name: "https-server"
    host: "0.0.0.0"
    port: 443
    protocol: "https"
    enabled: true
    tls:
      cert_file: "/path/to/cert.pem"
      key_file: "/path/to/key.pem"

# 域名配置
domains:
  - names: ["example.com", "www.example.com"]
    backends: ["127.0.0.1:8080", "127.0.0.1:8081"]
    protocols: ["http", "https"]
    load_balancer:
      algorithm: "round_robin"
    
  - names: ["api.example.com"]
    backends: ["127.0.0.1:9000"]
    protocols: ["http", "https", "websocket"]

# 功能特性配置
features:
  http3:
    enabled: true
    port: 443
  
  websocket:
    enabled: true
    timeout: "5m"
    buffer_size: 4096
  
  gzip:
    enabled: true
    level: 6
```

### 环境变量覆盖

```bash
# 覆盖服务器端口
export SANDWICH_SERVER_0_PORT=8080

# 覆盖 HTTP/3 配置
export SANDWICH_FEATURES_HTTP3_ENABLED=false

# 覆盖域名后端
export SANDWICH_DOMAIN_0_BACKENDS="127.0.0.1:9090,127.0.0.1:9091"
```

## 从 v1 迁移到 v2

### 1. 兼容性说明

- v2 版本保持了与 v1 版本的核心代理逻辑兼容
- 原有的环境变量配置仍然有效
- 可以逐步迁移到新的配置文件格式

### 2. 迁移步骤

#### 步骤 1: 备份现有配置

```bash
# 备份当前的环境变量配置
env | grep SANDWICH > sandwich_v1_env.backup
```

#### 步骤 2: 创建 v2 配置文件

基于现有的环境变量创建对应的 YAML 配置文件：

```yaml
# 将原有的 SANDWICH_HOST 和 SANDWICH_PORT 转换为
servers:
  - name: "main-server"
    host: "${SANDWICH_HOST:-0.0.0.0}"
    port: ${SANDWICH_PORT:-80}
    protocol: "http"
    enabled: true
```

#### 步骤 3: 测试新版本

```bash
# 在测试环境中启动 v2 版本
go run sandwich_v2.go --config=config.yaml

# 验证功能正常
curl -H "Host: your-domain.com" http://localhost:80/
```

#### 步骤 4: 生产环境部署

```bash
# 停止 v1 版本
pkill sandwich

# 启动 v2 版本
./sandwich_v2 --config=production.yaml
```

### 3. 配置映射对照表

| v1 环境变量 | v2 配置路径 | 说明 |
|------------|------------|------|
| `SANDWICH_HOST` | `servers[0].host` | 服务器监听地址 |
| `SANDWICH_PORT` | `servers[0].port` | 服务器监听端口 |
| `SANDWICH_LOG_LEVEL` | `logging.level` | 日志级别 |
| `SANDWICH_MONGO_URI` | `database.mongo.uri` | MongoDB 连接字符串 |
| `SANDWICH_INFLUX_URI` | `monitoring.influx.uri` | InfluxDB 连接字符串 |

## 新功能使用指南

### 1. 启用 HTTP/3 支持

```yaml
features:
  http3:
    enabled: true
    port: 443  # HTTP/3 端口
    cert_file: "/path/to/cert.pem"
    key_file: "/path/to/key.pem"
```

### 2. 启用 WebSocket 支持

```yaml
features:
  websocket:
    enabled: true
    timeout: "5m"        # 连接超时
    buffer_size: 4096    # 缓冲区大小
    ping_interval: "30s" # 心跳间隔
```

### 3. 配置多端口监听

```yaml
servers:
  # HTTP 服务器
  - name: "http-server"
    host: "0.0.0.0"
    port: 80
    protocol: "http"
    enabled: true
  
  # HTTPS 服务器
  - name: "https-server"
    host: "0.0.0.0"
    port: 443
    protocol: "https"
    enabled: true
    tls:
      cert_file: "/etc/ssl/certs/server.crt"
      key_file: "/etc/ssl/private/server.key"
  
  # API 服务器
  - name: "api-server"
    host: "0.0.0.0"
    port: 8443
    protocol: "https"
    enabled: true
    tls:
      cert_file: "/etc/ssl/certs/api.crt"
      key_file: "/etc/ssl/private/api.key"
```

### 4. 配置域名绑定

```yaml
domains:
  # 主站域名
  - names: ["example.com", "www.example.com"]
    backends: ["127.0.0.1:8080", "127.0.0.1:8081"]
    protocols: ["http", "https"]
    load_balancer:
      algorithm: "round_robin"
      health_check:
        enabled: true
        interval: "30s"
        timeout: "5s"
  
  # API 域名
  - names: ["api.example.com"]
    backends: ["127.0.0.1:9000"]
    protocols: ["http", "https", "websocket"]
    middleware:
      - "cors"
      - "rate_limit"
  
  # WebSocket 域名
  - names: ["ws.example.com"]
    backends: ["127.0.0.1:9001"]
    protocols: ["websocket"]
```

## 监控和运维

### 1. 健康检查

```bash
# 检查服务状态
curl http://localhost:80/health

# 检查统计信息
curl http://localhost:80/stats
```

### 2. 日志监控

```bash
# 查看实时日志
tail -f /var/log/sandwich/sandwich.log

# 过滤错误日志
grep "ERROR" /var/log/sandwich/sandwich.log
```

### 3. 配置热重载

```bash
# 发送 SIGHUP 信号重载配置
kill -HUP $(pgrep sandwich_v2)

# 或者使用管理接口
curl -X POST http://localhost:80/admin/reload
```

## 故障排除

### 1. 常见问题

#### 问题：服务启动失败

```bash
# 检查端口是否被占用
netstat -tlnp | grep :80

# 检查配置文件语法
./sandwich_v2 --config=config.yaml --validate
```

#### 问题：域名解析失败

```bash
# 检查域名配置
grep -A 5 "domains:" config.yaml

# 测试域名解析
curl -H "Host: example.com" http://localhost:80/
```

#### 问题：后端连接失败

```bash
# 检查后端服务状态
curl http://127.0.0.1:8080/health

# 检查网络连接
telnet 127.0.0.1 8080
```

### 2. 调试模式

```bash
# 启用调试日志
export SANDWICH_LOG_LEVEL=debug
./sandwich_v2 --config=config.yaml

# 或者在配置文件中设置
logging:
  level: debug
  format: json
```

## 性能优化

### 1. 连接池配置

```yaml
performance:
  max_connections: 10000
  connection_timeout: "30s"
  keep_alive_timeout: "60s"
  read_timeout: "30s"
  write_timeout: "30s"
```

### 2. 缓存配置

```yaml
cache:
  enabled: true
  ttl: "5m"
  max_size: "100MB"
  compression: true
```

### 3. 负载均衡优化

```yaml
domains:
  - names: ["example.com"]
    backends: ["127.0.0.1:8080", "127.0.0.1:8081", "127.0.0.1:8082"]
    load_balancer:
      algorithm: "least_connections"  # 最少连接数算法
      health_check:
        enabled: true
        interval: "10s"
        timeout: "3s"
        retries: 3
```

## 安全配置

### 1. TLS 配置

```yaml
servers:
  - name: "https-server"
    port: 443
    protocol: "https"
    tls:
      cert_file: "/etc/ssl/certs/server.crt"
      key_file: "/etc/ssl/private/server.key"
      min_version: "1.2"
      max_version: "1.3"
      cipher_suites:
        - "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"
        - "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256"
```

### 2. 访问控制

```yaml
security:
  rate_limit:
    enabled: true
    requests_per_minute: 1000
    burst: 100
  
  ip_whitelist:
    - "127.0.0.1"
    - "192.168.1.0/24"
  
  headers:
    x_frame_options: "DENY"
    x_content_type_options: "nosniff"
    x_xss_protection: "1; mode=block"
```

## 总结

Sandwich v2 提供了更强大的功能和更灵活的配置选项，同时保持了与 v1 版本的兼容性。通过本指南，您可以：

1. 了解 v2 版本的新功能特性
2. 学会如何配置和使用新功能
3. 顺利从 v1 版本迁移到 v2 版本
4. 掌握监控、运维和故障排除技巧

如有任何问题，请参考项目文档或提交 Issue。