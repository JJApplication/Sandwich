# Sandwich 代理服务扩展需求设计文档

## 1. 项目概述

### 1.1 当前架构分析
Sandwich 是一个基于 Go 语言开发的反向代理服务，具有以下核心功能：
- HTTP 反向代理转发
- 域名到后端服务的映射
- 负载均衡（随机轮询算法）
- 流量控制和熔断机制
- MongoDB 数据存储
- InfluxDB 监控数据收集
- 静态资源缓存

### 1.2 当前技术栈
- **核心框架**: Go 标准库 net/http
- **代理实现**: net/http/httputil.ReverseProxy
- **数据库**: MongoDB
- **监控**: InfluxDB
- **配置**: 环境变量
- **负载均衡**: 随机轮询

## 2. 扩展需求分析

### 2.1 多端口监听需求
**目标**: 支持同时监听多个端口，处理不同协议的请求

**技术要求**:
- 同时监听 HTTP (80) 和 HTTPS (443) 端口
- 支持配置多个监听地址和端口
- 每个端口可以绑定不同的域名和后端服务
- 支持 TLS 证书配置

### 2.2 HTTP/3 协议支持
**目标**: 支持 HTTP/3 (QUIC) 协议的代理转发

**技术要求**:
- 基于 QUIC 协议的 HTTP/3 服务器
- HTTP/3 到 HTTP/1.1 或 HTTP/2 的协议转换
- 证书管理和 ALPN 协商
- 可选特性，通过配置启用

### 2.3 WebSocket 协议支持
**目标**: 支持 WebSocket 连接的代理转发

**技术要求**:
- WebSocket 握手处理
- 双向数据流转发
- 连接保持和心跳机制
- 负载均衡支持
- 可选特性，通过配置启用

### 2.4 配置系统重构
**目标**: 设计灵活的配置系统，支持复杂的多服务器配置

**技术要求**:
- 支持 YAML/JSON 配置文件
- 多服务器配置（端口、主机、IP）
- 域名与服务器绑定配置
- 中间件配置支持
- 热重载配置

## 3. 技术方案设计

### 3.1 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                    Sandwich 代理服务                        │
├─────────────────────────────────────────────────────────────┤
│  Config Manager (配置管理)                                  │
├─────────────────────────────────────────────────────────────┤
│  Multi-Port Listener (多端口监听器)                         │
│  ├── HTTP Server (80)                                      │
│  ├── HTTPS Server (443)                                    │
│  ├── HTTP/3 Server (443/UDP) [可选]                        │
│  └── Custom Ports                                          │
├─────────────────────────────────────────────────────────────┤
│  Protocol Handlers (协议处理器)                             │
│  ├── HTTP/HTTPS Handler                                    │
│  ├── WebSocket Handler [可选]                              │
│  └── HTTP/3 Handler [可选]                                 │
├─────────────────────────────────────────────────────────────┤
│  Core Proxy Engine (核心代理引擎)                           │
│  ├── Request Router                                        │
│  ├── Load Balancer                                         │
│  ├── Circuit Breaker                                       │
│  └── Flow Control                                          │
├─────────────────────────────────────────────────────────────┤
│  Backend Services (后端服务)                               │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 模块设计

#### 3.2.1 Config 包设计
**包路径**: `sandwich/config`

**核心结构**:
```go
type Config struct {
    Servers    []ServerConfig    `yaml:"servers"`
    Domains    []DomainConfig    `yaml:"domains"`
    Middleware []MiddlewareConfig `yaml:"middleware"`
    Features   FeatureConfig     `yaml:"features"`
}

type ServerConfig struct {
    Name     string `yaml:"name"`
    Host     string `yaml:"host"`
    Port     int    `yaml:"port"`
    Protocol string `yaml:"protocol"` // http, https, http3
    TLS      *TLSConfig `yaml:"tls,omitempty"`
}

type DomainConfig struct {
    Domain    string   `yaml:"domain"`
    Servers   []string `yaml:"servers"`
    Backends  []string `yaml:"backends"`
    LoadBalance string `yaml:"load_balance"`
}
```

#### 3.2.2 HTTP/3 包设计
**包路径**: `sandwich/http3`

**核心功能**:
- QUIC 服务器实现
- HTTP/3 请求解析
- 协议转换器
- 证书管理

#### 3.2.3 WebSocket 包设计
**包路径**: `sandwich/websocket`

**核心功能**:
- WebSocket 升级处理
- 连接池管理
- 消息转发
- 心跳机制

### 3.3 实现策略

#### 3.3.1 多端口监听实现
1. **服务器管理器**: 创建 `ServerManager` 管理多个 HTTP 服务器实例
2. **协程并发**: 每个端口在独立的协程中运行
3. **优雅关闭**: 支持信号处理和优雅关闭
4. **错误处理**: 单个服务器失败不影响其他服务器

#### 3.3.2 HTTP/3 实现策略
1. **第三方库**: 使用 `quic-go` 库实现 QUIC 协议
2. **协议适配**: 实现 HTTP/3 到 HTTP/1.1 的协议转换
3. **证书共享**: 与 HTTPS 服务器共享 TLS 证书
4. **可选启用**: 通过配置文件控制是否启用

#### 3.3.3 WebSocket 实现策略
1. **协议检测**: 检测 WebSocket 升级请求
2. **连接代理**: 建立客户端到后端的 WebSocket 连接
3. **数据转发**: 双向透明转发 WebSocket 消息
4. **连接管理**: 处理连接断开和重连

## 4. 配置文件设计

### 4.1 配置文件示例 (config.yaml)
```yaml
# 服务器配置
servers:
  - name: "http-server"
    host: "0.0.0.0"
    port: 80
    protocol: "http"
  
  - name: "https-server"
    host: "0.0.0.0"
    port: 443
    protocol: "https"
    tls:
      cert_file: "/path/to/cert.pem"
      key_file: "/path/to/key.pem"
  
  - name: "http3-server"
    host: "0.0.0.0"
    port: 443
    protocol: "http3"
    tls:
      cert_file: "/path/to/cert.pem"
      key_file: "/path/to/key.pem"

# 域名配置
domains:
  - domain: "api.example.com"
    servers: ["https-server", "http3-server"]
    backends: ["127.0.0.1:8080", "127.0.0.1:8081"]
    load_balance: "round_robin"
    
  - domain: "ws.example.com"
    servers: ["https-server"]
    backends: ["127.0.0.1:9090"]
    protocols: ["websocket"]

# 功能特性配置
features:
  http3:
    enabled: true
    max_connections: 1000
  
  websocket:
    enabled: true
    ping_interval: 30s
    max_message_size: 1048576

# 中间件配置
middleware:
  - name: "rate_limit"
    enabled: true
    config:
      requests_per_second: 100
  
  - name: "circuit_breaker"
    enabled: true
    config:
      failure_threshold: 5
      reset_timeout: 60s
```

## 5. 兼容性考虑

### 5.1 向后兼容
- 保持现有环境变量配置的支持
- 现有 API 接口不变
- 数据库结构保持兼容

### 5.2 渐进式升级
- 新功能作为可选特性
- 配置文件优先级高于环境变量
- 支持配置文件和环境变量混合使用

## 6. 性能考虑

### 6.1 内存优化
- 连接池复用
- 缓冲区管理
- 及时释放资源

### 6.2 并发优化
- 协程池管理
- 锁粒度优化
- 无锁数据结构

### 6.3 网络优化
- TCP 连接复用
- HTTP/2 多路复用
- QUIC 连接优化

## 7. 安全考虑

### 7.1 TLS 安全
- 支持最新 TLS 版本
- 证书自动更新
- 安全密码套件

### 7.2 访问控制
- IP 白名单/黑名单
- 域名访问控制
- 请求频率限制

### 7.3 数据安全
- 敏感配置加密
- 日志脱敏
- 安全审计

## 8. 监控和日志

### 8.1 指标监控
- 连接数统计
- 请求响应时间
- 错误率统计
- 协议分布统计

### 8.2 日志增强
- 结构化日志
- 协议标识
- 链路追踪
- 性能分析

## 9. 部署和运维

### 9.1 配置管理
- 配置文件验证
- 热重载机制
- 配置版本管理

### 9.2 健康检查
- 服务健康状态
- 后端服务检查
- 自动故障转移

## 10. 测试策略

### 10.1 单元测试
- 各模块独立测试
- 配置解析测试
- 协议转换测试

### 10.2 集成测试
- 多协议并发测试
- 负载均衡测试
- 故障恢复测试

### 10.3 性能测试
- 并发连接测试
- 吞吐量测试
- 延迟测试

---

**文档版本**: v1.0  
**创建日期**: 2025年1月  
**最后更新**: 2025年1月